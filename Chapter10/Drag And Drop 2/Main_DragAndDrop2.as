package {	import flash.display.MovieClip;	import flash.events.Event;	import flash.events.MouseEvent;	public class Main_DragAndDrop2 extends MovieClip	{		private var _easing:Number;				public function Main_DragAndDrop2()		{			//Initialise variables			_easing = 0.3;						//Add event listeners			addEventListener(Event.ENTER_FRAME,onEnterFrame);		}		private function onEnterFrame(event:Event):void		{			if (redSquare.hitTestObject(redTarget))			{				if (! redSquare.isBeingDragged)				{					redSquare.x += (redTarget.x - redSquare.x) * _easing;					redSquare.y += (redTarget.y - redSquare.y) * _easing;				}			}			if (blueSquare.hitTestObject(blueTarget))			{				if (! blueSquare.isBeingDragged)				{					blueSquare.x += (blueTarget.x - blueSquare.x) * _easing;					blueSquare.y += (blueTarget.y - blueSquare.y) * _easing;				}			}		}	}}