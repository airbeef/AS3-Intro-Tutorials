package {	import flash.display.MovieClip;	import flash.events.Event;	import flash.events.MouseEvent;	public class Main_EasingWithMouse extends MovieClip	{		//Declare constants		private const EASING:Number = 0.1;				//Declare variables		private var _targetX:Number;		private var _targetY:Number;		public function Main_EasingWithMouse()		{			//Initialize variables			_targetX = player.x;			_targetY = player.y;			//Add event listeners			stage.addEventListener(MouseEvent.MOUSE_DOWN, onMouseDown);		}		private function onEnterFrame(event:Event):void		{			//Calculate the distance from the player to the mouse			var dx:Number = _targetX - player.x;			var dy:Number = _targetY - player.y;			var distance = Math.sqrt(dx * dx + dy * dy);						//Move the object if it is more than 1 pixel away from the mouse			if (distance >= 1)			{				player.x += (_targetX - player.x) * EASING;				player.y += (_targetY - player.y) * EASING;			}			else			{				trace("Player reached target")				removeEventListener(Event.ENTER_FRAME,onEnterFrame);			}		}		private function onMouseDown(event:Event):void		{			_targetX=mouseX;			_targetY=mouseY;			addEventListener(Event.ENTER_FRAME,onEnterFrame);		}	}}