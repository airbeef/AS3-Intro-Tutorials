package {	import flash.display.MovieClip;	import flash.events.Event;	import flash.events.TimerEvent;	import flash.utils.Timer;	public class Main_Timer extends MovieClip	{		private var _timer:Timer;		public function Main_Timer()		{			//Initialize timer			_timer = new Timer(1000);			_timer.addEventListener(TimerEvent.TIMER, onUpdateTime);			_timer.start();		}		private function onUpdateTime(event:Event):void		{			timeDisplay.text = String(_timer.currentCount);			//Stop timer when it reaches 10			if (_timer.currentCount == 10)			{				_timer.reset();				_timer.start();			}		}	}}